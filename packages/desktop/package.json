{
  "name": "@warpcore/desktop",
  "private": true,
  "version": "0.6.19",
  "scripts": {
    "dev": "cargo tauri dev",
    "prepare:binaries": "node scripts/prepare.mjs",
    "build": "npm run prepare:binaries && cargo tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
